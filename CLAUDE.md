# 九宫起卦阵 (Bagua Tower) — 项目统筹

> 本文件是项目「宪法」：全局约定与铁律。详细设计一律以 `九宫起卦阵-MVP-GDD.md` 为准，本文件不复制其内容。

## 项目快览

- 定位：周易卦象 × 环绕塔防 × 轻肉鸽构筑的小游戏（MVP）。
- 本质：用塔防当「检测器」的空间构筑肉鸽；核心卖点是「相邻成阵」。
- 当前阶段：**设计已定稿，尚未开始编码**，工程脚手架待搭。
- 目标：先在 PC 浏览器验证 core，后续再考虑上抖音 / 微信小游戏。

## 全局规则

- CLAUDE 输出一律用中文。
- 重要:当提示词含「仅回答」字样时,只输出回答,不要修改代码、不要动代码。
- `TODO.md` 按 Phase 分步组织,只记录功能点和子任务;不记录代码片段、测试环节、优化建议、
  估时、风险说明。严禁使用 emoji。完成标 `[x]`,未完成标 `[ ]`。
- 标记 `TODO.md` 任务为已完成前,必须核查对应代码有真实实现 —— 不是简化方案、不是 todo 占位。
- Git commit message 不写 Phase 编号,直接描述改动内容。

## 文档地图（改动前先读对应文档）

- `九宫起卦阵-MVP-GDD.md` — **设计真相源**。机制 / 数值 / 架构 / 技术决策全在此。要改机制，先改它，再改代码。
- `design-doc1.md` — 最初的头脑风暴稿，仅历史参考，**勿据此实现**（多数已被 MVP 砍掉）。
- `TODO.md` — 任务分步清单（约定见「全局规则」）。
- `CLAUDE.md` — 本文件。

## 技术栈（详见 GDD §16）

- 渲染 PixiJS v8 ｜ 语言 TypeScript ｜ 构建 Vite ｜ 包管理 **pnpm** ｜ 环境 WSL2 / Linux。
- 不用 Cocos：其编辑器无 Linux 版，且 MVP 优先代码迭代速度。
- 常用命令（工程搭好后）：
  - `pnpm install` — 装依赖
  - `pnpm dev` — 启动开发服务器（已配 host，WSL2 下 Windows 浏览器可访问）
  - `pnpm typecheck` — 类型检查
  - `pnpm build` — 构建

## 架构铁律（详见 GDD §11 / §16）

三层分离，**这是最重要的工程约束**：

- `src/core/` — 游戏逻辑（combo 引擎 / 放置 / 战斗 / 波次 / RNG）。**纯 TS，零渲染依赖**。
  core 内**严禁 import 任何渲染库**（pixi.js 等），以保证将来可整体移植到 Cocos。
- `src/data/` — 配置表（卦 / combo / 词条参数 / 全局参数），**策划可热改**。
- `src/view/` — Pixi 渲染层，**只订阅 core 状态来画**，不持有游戏逻辑。

派生规则：

- 所有数值与组合放 `data/`，逻辑层**只读不写死**。
- combo 不是硬编码特效，而是 **5 个词条动词**（onHit / onKill / statMod / applyStatus / zone）的配置组合。
  新增 combo = 加配置，不加代码分支。
- 同卦相邻不触发 combo（同卦走「原地叠级」，GDD §6.1）。

## 代码规范

- TypeScript `strict` 全开；core 逻辑层**不接受 `any`**，类型要显式。
- 命名：文件 camelCase，类型 PascalCase，卦 / combo 的 id 用拼音小写（`li`、`zhen`…）。
- 注释可用中文；解释「为什么」而非「做了什么」。
- 量化细节（行宽、缩进等）交给 linter / formatter，不在此约束。

## 工作流

- **先文档后代码**：改动任何机制，先更新 GDD 对应章节，再实现，再勾 `TODO.md`。
- **不脑补待定项**：GDD §14 列了未锁设计（败北条件 / 方位含义 / 数值原型 / 起卦经济）。
  遇到这些**先问用户，不要自行拍板**。
- **Git**：当前在默认分支时，动手前先开新分支，不直接提交到 main。
- 改完逻辑跑 `pnpm typecheck` 再交付。

## 禁区（Out of Scope）

- 不擅自实现 GDD §14 的待定项 —— 先与用户确认。
- 不碰 `.git` 历史、不强推。
- 不引入强数值付费；文案不宣称真实预测（合规红线，GDD §1）。
- 不在仓库写入密钥 / 敏感信息。
