# 《九宫起卦阵》MVP 游戏设计文档（GDD）

| 字段 | 内容 |
|---|---|
| 文档版本 | v0.1（MVP 核心机制定稿） |
| 日期 | 2026-06-24 |
| 状态 | 🟢 核心机制已锁 / 🟡 部分系统待定 |
| 范围 | 仅 MVP 方案 1（验证「相邻成阵」核心假设） |
| 工作标题 | 《九宫起卦阵》 |

> 本文档只记录**已经过讨论锁定**的机制。未定项集中在 §13「待定项」，不要在此前的章节里自行脑补。

---

## 1. 一句话定位与设计支柱

> **一款「周易卦象 × 环绕塔防 × 轻肉鸽构筑」游戏。玩家起卦抽出卦象塔，摆进战场，靠「相邻成阵」拼出会爆屏的阵法，抵御环绕进攻的厄运怪。**

本质上它**不是塔防，是用塔防当检测器的「空间构筑肉鸽」**。塔防只负责验证「你拼的阵成不成立」。

### 三根设计支柱（任何改动都要回头对照）

```
支柱 1  玄学是皮肤，不是机制
        八卦=8种伤害类型，飞星=波次buff，奇门=风险事件。
        剥掉皮，底下是被验证过的肉鸽框架。
        → 不懂玄学也能爽，懂玄学的人觉得妙。

支柱 2  星星是「空间里的阵法」，不是「单卡数值」
        联动组合(走宽)是主角，单卡升级(走高)是配角且故意做弱。
        空间联动层做厚，单卡数值层做薄。

支柱 3  位置必须是真策略，不是假象
        通过「环绕布局 + 射程 + 相邻combo」三重叠加，
        让「这个卦摆哪一格」成为每一步的真实决策。
```

### 合规红线

所有文案为「娱乐化命局挑战」，**不宣称真实预测**财运、疾病、婚恋或人生结果。怪物用轻 meme 化设定（厄运怪等），避免沉重宗教感。

---

## 2. MVP 目标与验证假设

MVP 唯一要回答的问题：

> **「相邻成阵」是否足够爽、且能被玩家自然理解？玩家会不会为了凑阵主动规划站位？**

只要这个假设成立，项目就有机会。飞星、奇门、64 卦、重卦等全部是**第二阶段的保鲜剂**，不进 MVP。

---

## 3. 核心循环

```
        ┌──────────────────────────────────────────────────┐
        │                                                  │
        ▼                                                  │
   ┌─────────┐  灵气足够   ┌─────────┐   拖拽    ┌─────────┐  │
   │  起卦   │ ──────────▶│ 暂存区  │ ───────▶ │  放卦   │  │
   │消耗灵气 │            │ 待放置  │          │落子/叠级│  │
   └─────────┘            └─────────┘          └────┬────┘  │
                                                    │相邻    │
                                                    ▼       │
   ┌─────────┐  清完一波   ┌─────────┐   自动     ┌─────────┐ │
   │  续局   │◀───────────│  守波   │◀───────── │  连阵   │ │
   │ 回灵气  │            │卦自动打 │   combo   │触发词条 │ │
   │ 续布阵  │            │环绕的怪 │           │爆特效   │ │
   └────┬────┘            └─────────┘           └─────────┘ │
        │                                                  │
        └──────────────── 进入下一波 ──────────────────────┘
```

| 环节 | 玩家动作 | 系统反应 |
|---|---|---|
| 起卦 | 点底部按钮（消耗灵气） | 随机产出 1 个基础卦 → 暂存区 |
| 放卦 | 拖暂存卦到格子 | 落子 / 叠级（见 §7 拖拽语法） |
| 连阵 | （被动，源于放卦位置） | 相邻卦触发 combo 词条 |
| 守波 | （观战） | 卦自动攻击环绕的怪 |
| 续局 | 起卦 / 合成续阵 | 回灵气，继续布阵迎下一波（无三选一，成长靠起卦 + 合成）|

---

## 4. 战场设计：4×10 环绕塔防

### 布局

放置区为 **4 行 × 10 列 = 40 格**。怪物在该区域**外围环绕跑圈**，作为移动靶。卦摆在格子里**朝外打圈上的怪**。没有「怪穿过地图」。

```
   ◀── 怪 ◀── 怪 ◀── 怪 ◀── 怪 ◀── 怪 ◀── 怪 ◀── 怪 ◀──
  ║                                                    ║
  ▼   ┌────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐ ▲
  怪  │    │    │    │    │    │    │    │    │    │    │R1 怪
  ▼   ├────┼────┼────┼────┼────┼────┼────┼────┼────┼────┤ ▲
  怪  │    │    │    │    │    │    │    │    │    │    │R2 怪
  ▼   ├────┼────┼────┼────┼────┼────┼────┼────┼────┼────┤ ▲
  怪  │    │    │    │    │    │    │    │    │    │    │R3 怪
  ▼   ├────┼────┼────┼────┼────┼────┼────┼────┼────┼────┤ ▲
  怪  │    │    │    │    │    │    │    │    │    │    │R4 怪
  ▼   └────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘ ▲
  ║    C1   C2   C3   C4   C5   C6   C7   C8   C9   C10   ║
   ──▶ 怪 ──▶ 怪 ──▶ 怪 ──▶ 怪 ──▶ 怪 ──▶ 怪 ──▶ 怪 ──▶
```

### 九宫方位（MVP 阶段休眠）

40 格之上**叠加一层 3×3 = 9 个「方位区」**（乾坤震巽坎离艮兑 + 中宫）。该层在 MVP **只做视觉分区与标签，不产生 buff**。它是为第二阶段的飞星/奇门预留的接口。`参见 §14 待定项①`

### 目标形态：放射八卦盘（MVP 后实现）

**MVP 用 4×10 长方形**（最省事、最快验证「相邻成阵」核心假设）。但**目标形态是放射 8 扇区八卦盘**，在打磨阶段替换：

- 8 个扇区 = 八宫（乾坤震巽坎离艮兑），环 = 纵深，圆心 = 中宫；怪绕外圈跑。
- 一次解决多个问题：① 八宫字面化，与第二阶段「方位层」完美咬合（每扇区一宫）；② 修掉 §5「纵深太浅」隐患——放射盘内环天然远离怪圈、外环天然贴近，中心 vs 边缘张力变真实且对称；③ 环绕更自然；④ 近正方形，竖屏适配优于 10 宽长条。
- 选**八边形 / 放射 8 扇区**而非纯圆：8 边对应八卦、易铺格子；外轮廓圆滑与否是表现层的事。
- 换形状代价很低：见 §11「Grid 拓扑接口化」——不动 combo 引擎与 data。

---

## 5. 战斗机制：卦 = 有射程的自动炮台

| 规则 | 定义 |
|---|---|
| 攻击方式 | 每个卦按**自身冷却**自动攻击**射程内最近的怪**，无需玩家手动操作（割草式）。 |
| 射程 | 每个卦有射程属性（长 / 中 / 短）。射程**决定一个卦能在哪些格子有效输出**。 |
| 位置作用 | 位置不再只是装饰，而是同时为三件事服务（见下）。 |

### 位置的三重意义

```
① combo 邻接    要联动就得把卦聚到一起
② 射程 × 覆盖   短射程必须贴外圈/繁忙弧段；长射程可坐深处仍够得着怪圈
③ 方位区 buff   (第二阶段) 这一局这块区域旺什么
```

### 涌现张力：阵眼 vs 莽夫

`走宽` 要求邻居多（中心格邻居最多），`射程/输出` 要求贴近怪圈（边缘格离怪近）。两个「最优位置」相反，于是射程自动把卦分成两种职业：

```
长射程卦  →  坐中心也够得着怪圈  →  中心邻居多  →  「阵眼 core」(combo 枢纽)
短射程卦  →  必须摆边缘才打得到  →  边缘邻居少  →  「边缘莽夫」(高输出, 少 combo)
```

**关键正反馈环**：「有的 combo 能增强射程」→ 短射程卦凑到该 combo 后可以往中心挪 → 吃到更多 combo。即：

```
凑出射程 combo ──▶ 解锁更好的站位 ──▶ 吃到更多 combo ──▶ 更强
       ▲                                              │
       └──────────────────────────────────────────────┘
```

> ⚠️ 设计风险（见 §14）：4 行的盘**纵向纵深只有 2 格**，「中心远离怪圈」在竖直方向较弱，张力主要来自**横向 + 弧段覆盖**。**根本解法是 §4 的放射八卦盘目标形态**；MVP 阶段先靠射程数值调校缓解。

---

## 6. 构筑系统：两根轴

```
        走宽 (横向 · 主角 · 质变) ─────────────────────▶
        不同卦相邻 = combo 词条 = 新行为/连锁/区域/状态
   ┌──────────────────────────────────────────────────┐
 走 │   乾 ──── 离 ──── 震                              │
 高 │   │       │ (火雷阵)                             │   两轴相乘:
 │  │   坎      巽                                     │   高级卦在阵里
 纵 │  (Lv4)                                          │   → 词条 proc 更猛
 向 │                                                 │   → 走高放大走宽
 │  │   自走棋合成 = 纯数值 = 量变 (次线性, Lv4 封顶)    │
 配 │                                                 │
 角 ▼                                                 │
   └──────────────────────────────────────────────────┘
```

### 6.1 走高（纵向 · 配角 · 故意做弱）

| 规则 | 定义 |
|---|---|
| 触发 | **自走棋合成**：两个同卦**同级**叠在一起 → 合成高一级，**消一格**（单位数 −1）。 |
| 等级链 | Lv1+Lv1→Lv2；Lv2+Lv2→Lv3；Lv3+Lv3→Lv4。封顶 **Lv4**（达 Lv4 需 8 个 Lv1 卦）。 |
| 效果 | 纯数值变大（更大/更快/更广的同一个攻击），**不解锁新技能**。 |
| 曲线 | **次线性**（占位，待 §14③）。 |
| 角色 | **不是走宽的替代品，是走宽的放大器**。词条随攻击数值放大，故高级卦让 combo 更猛。 |

> 设计理由：合成消格 = 用 combo 边（走宽名额）换单卡强度，叠加指数级成本（Lv4 = 8 卦），让走高保持配角、不喧宾夺主。
> 注：此为试玩后改版，推翻早期「原地叠级、Lv3 封顶」方案。

### 6.2 走宽（横向 · 主角）

不同卦相邻即触发 combo，详见 §8 词条系统。

---

## 7. 交互设计：拖拽语法

```
拖拽来源：暂存卦 / 盘上已有卦（盘上卦可随时拖动换位）

落到……        空格          同卦同级          其他占用格
──────────────────────────────────────────────────────────
来自暂存区     放置 Lv1       合成（→高一级）    非法 · 弹回
来自盘上       移动           合成（→高一级）    交换位置
──────────────────────────────────────────────────────────
任一成功操作后 → 触发邻接重扫 → combo 更新
```

合成 = 自走棋制（§6.1）：两个同卦**同级**合成高一级、消一格。

MVP 玩家动作只保留：① 点击起卦 ② 拖拽放置 / 移动 / 合成。

---

## 8. 核心系统：Combo / 词条引擎（模型 B · 叠加挂载）

### 8.1 模型定义

```
combo = 一条「相邻无序卦对」{A, B}。
这条边给【双方各发一个词条 rider】，词条挂在【卦的基础攻击】上。
基础攻击永远保留，词条只是给它加挂效果 → 越拼越满屏，加法结构，单卡永远有用。
```

示例：
```
边 {离, 震}「火雷阵」:
   → 给 离 挂:  命中时额外炸一道闪电   (onHit)
   → 给 震 挂:  命中时地上留一小片火     (onHit + zone)
```

### 8.2 几何封顶（天然平衡器）

一个卦正交相邻**最多 4 个邻居 → 最多扛 4 条词条**。无需人为设上限。副产品：**越靠中心邻居越多越强**，把「九宫/阵眼」从装饰变成真机制。

### 8.3 五个词条动词（引擎只需实现这 5 个）

| 动词 | 含义 | 例 |
|---|---|---|
| `onHit` | 基础攻击命中时，再放一个次级效果 | 火球→额外闪电 |
| `onKill` | 击杀时生成物体 | 击杀→掉铜钱 / 小爆炸 |
| `statMod` | 改基础攻击数值（伤害/范围/**射程**/攻速/弹跳） | 射程 +1 |
| `applyStatus` | 给怪挂 debuff | 减速 / 灼烧 DoT / 标记 |
| `zone` | 地上留持续区域 | 火地 / 冰墙 |

> **架构命门**：combo 不是「效果」，而是「几个动词的配置组合」。引擎实现 5 个动词，所有 combo 退化成**数据配置表（策划可热改）**。这是系统可扩展到 16/64 卦而不崩的根本。
> `{A,A}` 同卦对复用同一框架（statMod 自增），即 §6.1 的原地叠级——零新增代码。

### 8.4 引擎数据流

```
┌──────────────────────────────────────────────────────────┐
│                      COMBO ENGINE                         │
│                                                          │
│  ┌───────────────┐   每次放卦/叠级后扫描                   │
│  │ 邻接扫描器     │ ─ 找出所有相邻无序卦对 {A,B}            │
│  │ AdjacencyScan │                                       │
│  └──────┬────────┘                                       │
│         │ 卦对列表                                        │
│         ▼                                                │
│  ┌───────────────┐   查表 (数据驱动)                      │
│  │ Combo 配置表   │ ─ {离,震} → [rider 配置...]            │
│  └──────┬────────┘                                       │
│         │ 激活的词条                                      │
│         ▼                                                │
│  ┌───────────────┐   挂到卦的攻击上 (Σ ≤ 4 条)            │
│  │ 词条挂载器     │ ─ 卦.activeRiders = 各边发来的词条之和  │
│  └──────┬────────┘                                       │
│         ▼                                                │
│  ┌────────────────────────────────────────────────┐     │
│  │              5 个词条动词执行器                   │     │
│  │  onHit │ onKill │ statMod │ applyStatus │ zone   │     │
│  └────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

---

## 9. 八卦定义（MVP 全部 8 个）

| 卦 | 五行 | 角色定位 | 攻击方式 | 射程 | 视觉 |
|---|---|---|---|---|---|
| 乾 | 金 | 单体高伤 · 阵眼 | 飞剑直线穿透 | **长** | 金色剑气 |
| 坤 | 土 | 召唤 / 防御 | 土偶阻挡 | **短** | 土墙 / 小兵 |
| 震 | 雷 | 连锁伤害 | 闪电弹跳 | 中 | 雷链 |
| 巽 | 风 | 攻速 / 多段 | 风刃高速切割 | **短** | 青色旋风 |
| 坎 | 水 | 控制 / 减速 | 水波减速 | 中 | 蓝色冰环 |
| 离 | 火 | AOE 爆发 · 阵眼 | 火球爆炸灼烧 | **长** | 火焰爆炸 |
| 艮 | 土 | 阻挡 / 反伤 | 山石砸落 | **短** | 岩石突起 |
| 兑 | 金 | 经济 / 暴击 | 铜钱弹 / 暴击 | 中 | 金币飞溅 |

射程分布：长(乾·离) / 中(震·坎·兑) / 短(巽·坤·艮)。

---

## 10. MVP Combo 组合（6 个，覆盖全部 8 卦）

| Combo | 卦对 | 效果（一句话） | 用到的动词 |
|---|---|---|---|
| 火雷阵 | 离 + 震 | 火球命中额外闪电 / 闪电留火片 | `onHit` |
| 水山阵 | 坎 + 艮 | 减速 + 生成冰墙，撞墙冰爆 | `applyStatus` + `zone` |
| 风火阵 | 巽 + 离 | **给短射程的巽 +射程**，火球变移动火龙卷 | `statMod`（射程展示款） |
| 泽天阵 | 兑 + 乾 | 暴击射金剑 / 击杀掉铜钱 | `onKill` + `onHit` |
| 地泽阵 | 坤 + 兑 | 召唤物击杀掉铜钱 | `onKill` |
| 水火阵 | 坎 + 离 | 冰火交替蒸汽爆 | `onHit` + `applyStatus` |

**覆盖校验**：乾坤震巽坎离艮兑 8 卦全部出现；5 个动词全部被用到（MVP 即可完整验证词条系统）。其余卦对暂无 combo。

设计原则：每个 combo 能一句话讲清 + 有明显视觉差异 + 让玩家期待「再来一个某卦」。

---

## 11. 系统架构

```
┌──────────────────────────────────────────────────────────┐
│                      表现层 (View)                        │
│  战场渲染 │ 卦/特效/粒子 │ 怪物动画 │ 拖拽UI │ 合成列表UI   │
└────────────────────────────┬─────────────────────────────┘
                             │ 状态订阅 / 事件
┌────────────────────────────┴─────────────────────────────┐
│                    游戏逻辑层 (Core)                       │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │
│  │ 起卦系统│ │ 放置系统│ │Combo引擎│ │ 战斗系统│         │
│  │ Gacha   │ │Placement│ │ Combo   │ │ Combat  │         │
│  └─────────┘ └─────────┘ └─────────┘ └────┬────┘         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐      │              │
│  │怪物/波次│ │经济/灵气│ │肉鸽Buff │      │              │
│  │ Wave    │ │ Economy │ │Roguelite│      │              │
│  └─────────┘ └─────────┘ └─────────┘      │              │
└────────────────────────────┬──────────────┴──────────────┘
                             │ 读取
┌────────────────────────────┴─────────────────────────────┐
│              数据配置层 (Data · 策划可热改)                 │
│  卦定义表 │ Combo配置表 │ 词条动词参数 │ 怪物表 │ 波次表    │
└──────────────────────────────────────────────────────────┘
```

> 架构原则：**所有数值与组合放在数据配置层**，逻辑层只读不写死。这样策划能在不改代码的前提下调平衡、加 combo。

> 形状无关原则：**Grid 暴露统一拓扑接口**（`neighbors(i)` 等），combo 引擎只调接口、不关心盘是方形还是放射。换形状（§4 放射八卦盘）只重写 Grid 实现 + View + 怪道，逻辑层与 data 一行不动；放射盘每格邻居仍 ≤4，§8.2「几何封顶」原样保留。

---

## 12. 核心数据模型（schema 草案）

```
Gua（卦实例）                Combo（组合配置）            Rider（词条）
──────────────             ──────────────             ──────────────
id        : "li"           pair   : ["li","zhen"]      verb   : onHit | onKill
name      : "离"            name   : "火雷阵"                    | statMod | applyStatus | zone
element   : 火              riders :                    target : self | enemy | ground
attackType: 火球              - to: "li",  rider:{...}   params : { ... }
baseAttack:                   - to: "zhen",rider:{...}   scalesWithLevel : true
  damage  : <num>
  cooldown: <sec>
  range   : long|mid|short  Cell（格子）                Enemy（怪）
  aoe     : <num>           ──────────────             ──────────────
level     : 1..3 (cap)      row, col                   hp, speed
activeRiders: [Rider...]    occupant : Gua | null      orbitPos (环绕位置 0~1)
   ↑ 每帧由 Combo 引擎重算   zone     : 方位ID(休眠)      statuses : [减速/灼烧/标记...]
```

---

## 13. MVP 范围边界

### ✅ 做

- 8 个基础卦（含各自射程）
- 6 个两两 combo
- 自走棋合成（Lv1–4）
- 4×10 环绕战场
- 起卦 + 拖拽放置 / 移动 / 合成（无三选一）
- Combo 引擎（5 动词，数据驱动）

### ❌ 不做（留第二阶段）

- 64 卦 / 带词条的稀有卦 / 具名重卦（乾为天等带新技能）
- **三连成阵 / 围中成局**（MVP 只做两两相邻）
- 飞星（按波变 buff）/ 奇门八门（事件）/ 梅花（每日种子）
- 九宫方位 buff（MVP 只保留视觉分区）
- 局外成长（图鉴 / 天赋 / 皮肤）

---

## 14. 待定项（尚未锁定，勿脑补）

| # | 待定项 | 说明 |
|---|---|---|
| ① | **九宫方位「每局不同」的确切含义** | 在 4×10 上是「方位区 buff 盘随机重排（位置向）」，还是「某方位当令的全局 buff（构筑方向向）」，还是「怪的环绕路线/入场每局变」？三种是不同的肉鸽旋钮，未定。 |
| ② | **败北条件**（试玩二轮已定） | 怪从**单一起点**刷出 → 沿环行进 → 抵达**终点**被抵消并扣核心耐久 → 耐久 0 判负。起点/终点在战场上标识。数值占位。 |
| ③ | **数值原型** | 8 卦的基础伤害/冷却/射程具体值、Lv 曲线、怪血量/移速/密度曲线、灵气产出与起卦成本。决定「30 秒手感」，未定。 |
| ④ | **起卦经济细节** | 灵气产出速率、暂存槽上限、起卦成本是否递增。 |

### 已知风险

- **4×10 长宽比使纵向纵深仅 2 格**，削弱「中心远离怪圈」的张力（§5）。射程数值需原型验证，必要时重审长宽比。
- 加入「方位每局不同」会把 MVP 验证目标从 1 个变成 2 个，需评估是否稀释对「combo 核心假设」的干净验证。

---

## 15. 后续路线（参考，非承诺）

```
Phase 1 (本 MVP)   验证「相邻成阵」爽 + 易懂
        │
Phase 2            点亮方位层: 飞星(按波buff) + 奇门(选门事件)
        │          三连成阵 / 围中成局 / 重卦(带新技能)
        │
Phase 3            扩展至 64 卦 / 稀有词条卦 / 梅花每日命局
        │          局外成长: 图鉴 + 轻天赋 + 皮肤(变现)
        ▼
        商业化: 激励视频(复活/重起卦/刷新) + 皮肤
```

---

## 16. 技术方案（已锁定）

### 决策

| 项 | 选择 | 理由 |
|---|---|---|
| 阶段定位 | **先验证 core，再考虑上平台** | MVP 只验「相邻成阵」假设，迭代速度优先 |
| 渲染框架 | **PixiJS v8** | 纯渲染、最轻、WSL2/Linux 原生运行、代码优先 |
| 语言 / 构建 | **TypeScript + Vite** | PC/WSL2 即时 HMR，类型安全 |
| 包管理 | **pnpm** | 快、省磁盘、依赖解析严格 |
| 开发环境 | **WSL2 / Linux** | 主力环境（Cocos 编辑器无 Linux 版，故 MVP 不选） |

### 为什么不是 Cocos（暂时）

- Cocos Creator 编辑器**官方仅 Windows / macOS**，与当前 WSL2/Linux 主力环境冲突。
- MVP 目标是「快速验证好不好玩」，编辑器工作流不如代码优先迭代快。
- 上抖音/微信小游戏是 Cocos 的强项，但那是**验证通过之后**的事。

### 去风险原则（最重要，凌驾于框架选择之上）

> **Core（combo 引擎 / 放置 / 战斗模拟 / 波次 / RNG）写成纯 TypeScript，零渲染依赖。**
> 渲染器只订阅状态来画。于是渲染框架**可替换**：将来若上抖音/微信，只需把 View 层
> 移植到 Cocos/Laya，最有价值、最难写的 Core **一行不动**。这条原则把框架选择去风险化了。

### 工程结构

```
bagua-tower/
├── index.html
├── package.json / tsconfig.json / vite.config.ts
├── 九宫起卦阵-MVP-GDD.md
└── src/
    ├── main.ts              入口：装配 Pixi + Core + View
    ├── core/   (纯 TS · 零渲染依赖 · 可移植)
    │   ├── types.ts         Gua/Combo/Rider 等 schema (§12)
    │   ├── rng.ts           确定性随机
    │   ├── grid.ts          4×10 格子 + 正交邻接
    │   ├── comboEngine.ts   邻接扫描→查表→挂词条 (§8)
    │   └── gameState.ts     顶层状态 + tick
    ├── data/   (配置表 · 策划可热改)
    │   ├── gua.ts           8 卦定义 (§9)
    │   ├── combos.ts        6 组合 (§10)
    │   └── config.ts        全局参数
    └── view/   (Pixi 渲染 · 只订阅 core 状态)
        └── BoardView.ts     战场 / 卦 / 成阵高亮
```

### 何时切换到 Cocos

当且仅当**同时满足**：① 已确定要发抖音/微信小游戏；② 有 Windows/Mac 开发机。
届时依「去风险原则」，只重写 View 层。

---

*本文档随讨论迭代，§14 待定项解决后升版至 v0.2。*
